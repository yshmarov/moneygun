<% content_for :title, t(".title") %>

<%= render "organizations/tabs", active_tab: :browse_organizations %>

<%= render SectionComponent.new do %>
  <%= render "organizations/table", organizations: @organizations %>
<% end %>
