<% content_for :title, t(".title") %>

<%= render "organizations/tabs", active_tab: :browse_organizations %>

<%= render PageComponent.new(full_width: true) do %>
  <%= render "organizations/table" %>
<% end %>
