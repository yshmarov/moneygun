<% content_for :title, t(".title") %>

<%= render PageComponent.new(title: @organization.name) do |component| %>
  <%= render "public/organizations/organization", organization: @organization %>
<% end %>
