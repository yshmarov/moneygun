<% content_for :title, @organization.name %>

<%= modal do %>
  <%= render PageComponent.new(full_width: false) do |component| %>
    <%= render "organizations/table", organizations: [@organization] %>
  <% end %>
<% end %>
