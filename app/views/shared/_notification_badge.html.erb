<% if count.positive? %>
  <span class="badge badge-error badge-xs font-bold leading-none">
    <span aria-hidden="true"><%= count > 99 ? "99+" : count %></span>
    <span class="sr-only"><%= count %> <%= t("users.notifications.index.title").downcase %></span>
  </span>
<% end %>
