<%= modal do %>
  <%= render PageComponent.new(title: t("organizations.transfers.show.title"), content_container: false, full_width: false) do |component| %>
    <%= render "organizations/transfers/form" %>
  <% end %>
<% end %>
