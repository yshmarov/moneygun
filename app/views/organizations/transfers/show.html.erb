<%= turbo_frame_tag "dialog_content" do %>
  <%= render PageComponent.new(title: t(".title"), content_container: false, full_width: false) do |component| %>
    <%= render "organizations/transfers/form" %>
    <%= link_to t("shared.actions.cancel"), organization_path(current_organization), class: "du-btn du-btn-secondary" %>
  <% end %>
<% end %>
