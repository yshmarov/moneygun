<%= modal do %>
  <%= render PageComponent.new(title: t("organizations.transfers.show.title"), full_width: false) do |component| %>
    <%= render "organizations/transfers/form" %>
  <% end %>
<% end %>
