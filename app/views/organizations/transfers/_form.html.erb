<%= form_with url: organization_transfer_path(Current.organization), method: :patch, class: "contents space-y-4" do |form| %>
  <div class="">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, Current.organization.users.where.not(users: { id: current_user.id }), :id, :email, { include_blank: true, required: true }, { class: "form-input" } %>
  </div>

  <div class="flex justify-end">
    <%= form.submit class: "du-btn du-btn-primary", data: { turbo_confirm: t("shared.actions.confirm") } %>
  </div>
<% end %>
