<% content_for :title, t("organizations.edit.title") %>

<%= modal do %>
  <%= render PageComponent.new(title: t("organizations.edit.title")) do |component| %>
    <%= render "form", organization: @organization %>
  <% end %>
<% end %>
