<% content_for :title, t("organizations.edit.title") %>

<%= render PageComponent.new(title: t("organizations.edit.title"), content_container: true, full_width: false) do |component| %>
  <%= render "form", organization: @organization %>
  <%= link_to t("shared.actions.cancel"), @organization, class: "btn btn-secondary" %>
<% end %>
