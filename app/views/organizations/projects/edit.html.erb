<% content_for :title, "Editing project" %>

<%= modal do %>
  <%= render SectionComponent.new(title: "Editing project") do |component| %>
    <%= render "form", project: @project %>
  <% end %>
<% end %>
