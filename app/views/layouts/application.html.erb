<!DOCTYPE html>
<html lang="en" class="h-full bg-base-300 text-base-content">
  <head>
    <%= render "layouts/head" %>
  </head>
  <body class="flex h-dvh overflow-hidden" data-controller="mobile-sidebar layout-switcher">
    <% sidebar_layout = cookies[:sidebar_layout] || "default" %>
    <% if sidebar_layout == "default" %>
      <%= render "shared/sidebar" %>
    <% else %>
      <%= render "shared/sidebar_2_cols" %>
    <% end %>

    <div class="flex-grow flex flex-col bg-base-300 min-w-0">
      <header class="sticky z-30 top-0">
        <%= render "shared/navbar" %>
        <div id="flash" class="fixed bottom-24 lg:bottom-6 right-4 z-40 flex flex-col items-end">
          <%= render "shared/flash" %>
        </div>
      </header>
      <main class="flex-grow p-4 bg-base-100 overflow-y-auto lg:m-2 lg:mt-0 lg:ml-0 lg:rounded-xl lg:border border-base-content/20">
        <%= yield %>
      </main>
      <%= render "shared/footer_nav" %>
    </div>
    <%= turbo_frame_tag :modal %>
  </body>
</html>
