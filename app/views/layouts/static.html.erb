<!DOCTYPE html>

<html lang="en" class="h-full bg-base-300 text-base-content">
  <head><%= render "layouts/head" %></head>

  <body class="flex flex-col min-h-screen">
    <div class="flex-grow flex flex-col bg-base-300">
      <header class="sticky z-20 top-0">
        <%= render "shared/unauthenticated_navbar" %>

        <div
          id="flash"
          role="alert"
          aria-live="polite"
          class="
            absolute left-1/2 -translate-x-1/2 z-40 flex flex-col
            items-center pointer-events-auto
          "
        >
          <%= render "shared/flash" %>
        </div>
      </header>

      <main class="flex-grow p-4 lg:p-10 bg-base-100"><%= yield %></main>

      <%= render "shared/footer" %>
    </div>

    <%= turbo_frame_tag :modal %>
  </body>
</html>
