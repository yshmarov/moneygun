<%= render PageComponent.new(title: t("devise.views.unlocks.new.title")) do %>
  <%= render "devise/shared/oauth_links" %>

  <%= form_with model: resource, as: resource_name, url: unlock_path(resource_name), method: :post, class: "space-y-4" do |f| %>
    <%= render "shared/errors", object: resource %>

    <div class="space-y-1">
      <%= f.label :email, t("devise.views.unlocks.new.email_label"), class: "block text-sm font-medium" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input", placeholder: t("devise.views.unlocks.new.email_placeholder") %>
      <p class="text-xs"><%= t("devise.views.unlocks.new.email_help") %></p>
    </div>

    <div class="pt-2">
      <%= f.submit t("devise.views.unlocks.new.submit"), class: "du-btn du-btn-primary du-btn-block" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
<% end %>
