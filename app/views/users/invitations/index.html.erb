<% content_for :title, t(".title") %>

<%= render "organizations/tabs", active_tab: :invitations %>

<%= render PageComponent.new(title: t("invitations.index.title"), full_width: true) do |component| %>
  <%= render "organizations/table" %>
<% end %>
