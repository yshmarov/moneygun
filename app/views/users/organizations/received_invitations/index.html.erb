<% content_for :title, t(".title") %>

<%= render "user/organizations/tabs", active_tab: :invitations %>

<%= render PageComponent.new do %>
  <%= render "user/organizations/table", organizations: @organizations %>
<% end %>
