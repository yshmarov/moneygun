<% content_for :title, t(".title") %>

<%= modal do %>
  <%= render PageComponent.new(title: t("static.privacy.title")) do |component| %>
    <div class="prose">
      <%= markdown_to_html(File.read(Rails.root.join('app/views/static/privacy.md'))) %>
    </div>
  <% end %>
<% end %>
