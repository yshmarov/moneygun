<% content_for :title, t(".title") %>

<%= modal do %>
  <%= render PageComponent.new(title: t("static.refunds.title")) do |component| %>
    <div class="prose">
      <%= markdown_to_html(File.read(Rails.root.join('app/views/static/refunds.md'))) %>
    </div>
  <% end %>
<% end %>
