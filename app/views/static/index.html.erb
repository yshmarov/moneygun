<% content_for :title, t(".title") %>

<%= modal do %>
  <%= render PageComponent.new(title: t("static.index.title")) do |component| %>
    <div class="prose">
      <%= markdown_to_html(File.read(Rails.root.join('README.md'))) %>
    </div>
  <% end %>
<% end %>
