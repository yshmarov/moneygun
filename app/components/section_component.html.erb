<section class="space-y-4 mx-auto <%= width_class %> @container">
  <% if title.present? || page_title.present? || action_list.present? || subtitle.present? %>
    <header class="space-y-1">
      <% if title.present? || page_title.present? || action_list.present? %>
        <div class="flex justify-between gap-4 flex-wrap">
          <div class="flex-1 min-w-0">
            <% if page_title.present? %>
              <%= page_title %>
            <% elsif title.present? %>
              <h1 class="text-base font-medium"><%= title %></h1>
            <% end %>
          </div>
          <% if action_list.present? %>
            <div class="flex flex-wrap items-center gap-2">
              <%= action_list %>
            </div>
          <% end %>
        </div>
      <% end %>

      <% if subtitle %>
        <p class="text-sm text-base-content/60"><%= subtitle %></p>
      <% end %>
    </header>
  <% end %>

  <div class="space-y-4">
    <%= content %>
  </div>
</section>
